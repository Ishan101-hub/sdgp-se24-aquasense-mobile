# AquaSense — Python dependencies (merged from both backends)

# Web framework
fastapi
uvicorn[standard]

# Database (async PostgreSQL / Supabase)
sqlalchemy
asyncpg

# Authentication
passlib[bcrypt]
python-jose[cryptography]

# Encryption (Kulith — phone/address at rest)
cryptography

# MQTT
aiomqtt

# Configuration
pydantic-settings
python-dotenv

# Rate limiting (Kulith)
slowapi

# HTTP client (Google OAuth)
httpx

email-validator

# PDF generation
reportlab
